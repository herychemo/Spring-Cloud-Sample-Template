
server:
  port: 9002

spring:
  application:
    name: hystrixdashboardsample
  config:
    name: hystrixdashboardsample

management:
  endpoints:
    web:
      exposure:
        include: '*'
      base-path: /actuator
  endpoint:
    health:
      show-details: always


turbine:
  aggregator:
    clusterConfig: USERS-MS-SAMPLE
  app-config: users-ms-sample
  instanceUrlSuffix:
    USERS-MS-SAMPLE: /users-ms-sample/actuator/hystrix.stream
  combine-host-port: true


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    instance:
      metadataMap:
        instanceId: ${spring.application.name}:${server.port}
    registerWithEureka: false
