spring:
  application:
    name: turbine-server
  cloud:
    config:
      enabled: true
      name: turbine-server
      uri: http://localhost:8888
logging:
  file: ${java.io.tmpdir}/logs/${spring.application.name}.log
