spring:
  application:
    name: accounts-ms
  cloud:
    config:
      enabled: true
      name: accounts-ms
      uri: http://localhost:8888
logging:
  file: ${java.io.tmpdir}/logs/${spring.application.name}.log