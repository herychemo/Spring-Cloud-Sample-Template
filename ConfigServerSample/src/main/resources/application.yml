

server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/herychemo/SampleConfigRepo.git
          #username: ${config_server_username}
          #password: ${config_server_password}
          skip-ssl-validation: true
          timeout: 6

