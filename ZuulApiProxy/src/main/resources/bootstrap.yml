#Configuration Server should overwrite this properties.
eureka.client.enabled: false
spring.cloud.config.enabled: false
---
spring.profiles: local,dockercli,dev,qa,perf,prod
spring:
  application:
    name: zuul-api-proxy
  cloud:
    config:
      enabled: true
      name: ZuulApiProxy
      uri: ${CONFIG_SERVER:http://localhost:8888}
logging:
  config: ${spring.cloud.config.uri}/${spring.cloud.config.name}/${spring.profiles.active:default}/master/logback-spring.xml
  file: ${java.io.tmpdir}/logs/${spring.application.name}.log
